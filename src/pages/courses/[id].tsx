import { useRouter } from "next/router"

export default function Course() {
const router = useRouter()

  //const course = courses[router.query.id]
  //if (!course) return <p></p>
    return (
        <div>course</div>
    )
}